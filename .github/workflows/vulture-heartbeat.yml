steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22' # Use 22 for maximum stability right now

      - name: Check Files
        run: ls -R # This will print all files in the log so we can see what is missing
