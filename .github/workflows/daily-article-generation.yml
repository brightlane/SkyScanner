name: SkyScanner Automation

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  generate-articles:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Generate articles (you already have this part)
      - name: Run article generation
        run: node ./generateArticlesWithKeywords.js

      # Commenting out the sitemap generation since you already have one
      # - name: Generate Sitemap
      #   run: node ./generateSitemap.js

      - name: Deploy to GitHub Pages
        run: |
          # Command to deploy your content, for example:
          # npx gh-pages -d ./public
          
      # You can add other deployment steps here if necessary.
