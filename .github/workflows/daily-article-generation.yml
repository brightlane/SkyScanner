name: Daily Article Generator and Deployment

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

jobs:
  generate-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Generate articles
        run: npm run generate

      # 🔥 CRITICAL FIX: remove npm noise BEFORE committing
      - name: Clean unwanted changes
        run: |
          git checkout -- package.json package-lock.json || true

      - name: Commit and push changes
        run: |
          echo "Checking changes..."

          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add -A

          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "Automated article update"
          git push
